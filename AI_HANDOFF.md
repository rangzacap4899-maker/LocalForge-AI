# LocalForge AI — handoff context

Last updated: 2026-08-07 (Asia/Bangkok) — sixth handoff

This file is the durable context for another AI assistant taking over after the
current assistant reaches a usage limit. Read the current source and `git diff`
before acting; this document explains intent and constraints but source code is
authoritative.

## User goal and communication

The user is building a polished, click-to-run local AI desktop workspace on
Bazzite/Fedora. The UI is primarily Thai but supports English, Chinese, and
Japanese. The user prefers direct implementation and real testing over plans or
repeated clarification. Reply in Thai unless asked otherwise.

Main repository:
`https://github.com/rangzacap4899-maker/LocalForge-AI.git`

## Machine and runtime

- CPU: Intel Xeon E5-2680 v3, 12 cores / 24 threads
- RAM: 15 GiB
- GPU: Radeon RX 470/480 (Ellesmere), 8 GiB VRAM, Vulkan/amdgpu
- OS: Bazzite/Fedora; do not suggest `apt`
- Build parallelism: use at most `-j4` to avoid exhausting RAM
- Python virtual environment: `.venv`
- llama.cpp server:
  `runtime/llama.cpp/build-vulkan/bin/llama-server`
- Application launcher: `./launch_localforge_ai.sh`
- Runtime state/logs: `~/.local/state/localforge-ai/`

Installed primary model files (ignored by Git):

- `models/gemma-4-e4b/gemma-4-E4B_q4_0-it.gguf`
  - 5,154,941,280 bytes
  - SHA-256 `676c35070db6dbe52f93e9c864ee0fba4eddea94b9c875d9cb10daff453fbaee`
- `models/gemma-4-e4b/gemma-4-E4B-it-mmproj.gguf`
  - 991,552,256 bytes
  - SHA-256 `7498a37cb619e55f2fcf87eb931f56e99389ed6d432e4c5c66110694c0d65578`

## Current product capabilities

- CustomTkinter chat UI with responsive layout, themes, UI scaling, Thai fonts
- Conversation storage/search/pin/export/delete and context meter
- Streaming output, cancellation, token/time/tokens-per-second display
- Copy, paste, selection, answer-copy and code-copy controls
- Workspace-scoped file tools, diff approval, backups and Undo
- Project Explorer and direct multi-file generation
- Web search/read tools
- Local model download/load/stop/delete/benchmark and automatic model routing
- Optional multi-agent coding workflow
- MCP stdio clients, permission hooks, audit log and secret redaction
- Languages: Thai, English, Simplified Chinese, Japanese
- Image selection and Wayland clipboard image paste (`Ctrl+Shift+V`)
- Gemma 4 image/audio requests through the official multimodal projector
- PipeWire microphone recording (`pw-record`, PCM WAV 16 kHz mono)
- Local answer speech using `spd-say`, with eSpeak fallback
- Image thumbnails inside user chat bubbles; media paths persist in history
- Complete i18n: all four catalogs share identical keys (test-enforced); the
  settings language picker now switches language live (rebuilds the UI) instead
  of requiring a restart
- Theme toggle stays in sync with saved preferences; language/theme changes apply
  immediately
- RAG/semantic-cache manager window (sidebar `🗂 จัดการ RAG & Cache`) showing
  counts and one-click clear of RAG chunks and semantic cache
- Multi-agent path now accepts media as a reference note appended to the request
- Saved workspace persists across restarts (settings `workspace`), fallback to
  `LOCALFORGE_WORKSPACE`/`GEMMA_WORKSPACE`, then `$HOME`; launcher no longer
  hardcodes `/home/addrang`
- `install.sh`: distro-aware installer (dnf/apt/pacman) that installs system
  packages, creates `.venv`, installs requirements, and writes/registers the
  desktop entry
- Context summarization (`_summarize_context`) now runs a real LLM summary via
  `GemmaClient.summarize()` in a background thread; the `send()` flow defers the
  request until the summary lands (`_pending_send`) and falls back to fast local
  truncation when no server is up. The status label is restored to `idle` after
  summarizing (previously it stayed stuck on `กำลังสรุปบริบท…`)
- The fallback summary prefix is localized (`summary_prefix` key)
- Previously dead i18n keys `change_folder`/`project_explorer`/`undo_short` are
  now wired as sidebar tooltips (`ToolTip` helper)

## Important source files

- `python/chatbot_app.py`: UI, model manager, API client, tools and multimodal UI
- `python/localforge_core.py`: conversation store, routing, model info, transactions
- `python/localforge_i18n.py`: all four language catalogs
- `python/localforge_mcp.py`: MCP stdio lifecycle and tool selection
- `python/localforge_hooks.py`: permissions, audit and redaction hooks
- `python/test_*.py`: unit tests (51 passing)
- `python/CHATBOT_README.md` and `README.md`: user documentation
- `packaging/localforge-ai.desktop`: desktop launcher entry (generated by `install.sh`)
- `install.sh`: system/venv/desktop installer
- `launch_localforge_ai.sh`: application launcher

## Gemma 4 E4B profile and recent bug history

`inference_profile()` in `python/chatbot_app.py` must keep these settings for
Gemma 4 E4B on this machine:

- `gpu_layers = all`
- context 8,192 (do not use the model's theoretical 128K on this hardware)
- Q8 K/V cache
- 12 inference and batch threads
- batch 2,048 and ubatch 2,048
- one slot, no continuous batching, reasoning disabled
- load sibling `gemma-4-E4B-it-mmproj.gguf` with `--mmproj` when present

Do not reduce multimodal ubatch to 256. A real pasted image caused llama.cpp to
abort with:
`non-causal attention requires n_ubatch >= n_tokens`.

Do not restore the streaming socket timeout to 5 seconds. Vision prefill can be
silent for 10–15 seconds, which caused false `timed out` errors and canceled
server tasks. `STREAM_IDLE_TIMEOUT_SECONDS` is currently 60 seconds.

Verified results after fixes:

- 3840x2160 JPEG, non-streaming: complete response in 10.42 s
- Same large JPEG through the UI streaming path: 33 chunks in 11.68 s
- Microphone capture produced valid PCM WAV, mono 16 kHz
- Silent microphone sample correctly returned `ไม่มีเสียงพูด`
- Unit suite: 40 tests passing at the time of the first handoff; 47 tests
  passing after the i18n/installer/RAG-manager work (a new
  `test_localforge_algorithms.py` covers `VectorDB.counts/clear_rag/clear_cache`
  and `cosine_similarity`)

Image/audio notes:

- OpenAI-compatible content types are `image_url` data URLs and `input_audio`.
- Put images before prompt text and audio after prompt text (`with_media`).
- Media is limited to 25 MB.
- Base64 data is transient and is not persisted; only local `media_paths` are.
- Thumbnails are PNG files cached in `~/.local/state/localforge-ai/thumbnails/`.
- Old conversations created before thumbnail support lack media paths and cannot
  reconstruct their images; the user must attach those images again.
- llama.cpp currently logs that Gemma 4 audio is experimental.

## Testing and diagnostics

Fast required checks after Python changes:

```bash
python3 -m py_compile python/*.py
.venv/bin/python -m unittest discover -s python -p 'test_*.py'
git diff --check
```

Quick headless boot check (no model server required):

```bash
xvfb-run -a .venv/bin/python -c \
  "import os,sys,tempfile; os.environ['XDG_STATE_HOME']=tempfile.mkdtemp(); \
   os.environ['LOCALFORGE_WORKSPACE']='/tmp'; sys.path.insert(0,'python'); \
   import chatbot_app as m; \
   m.messagebox.showerror=m.messagebox.showwarning=print; \
   a=m.ChatApp(); a.update(); a._select_language('English'); a.update(); a._on_close()"
```

Before starting an end-to-end server test, check whether the user's app already
owns port 8080:

```bash
pgrep -a llama-server
ss -ltnp | rg ':8080'
tail -120 ~/.local/state/localforge-ai/server.log
```

Do not kill a server belonging to the currently open user app merely to run a
test. Use another port only if VRAM permits; loading two Gemma 4 instances at
once normally does not fit. If a test starts its own manager, always stop it in
`finally` and confirm no test-owned server remains.

The screenshot/thumbnail path depends on ImageMagick (`magick`) and Tk
`PhotoImage`; Wayland clipboard paste depends on `wl-paste`; recording depends
on `pw-record`; speech output uses `spd-say` or eSpeak. These commands exist on
the development machine.

## Git and publishing workflow — critical

The local working tree originated from `google/gemma.cpp`, so remotes are:

- `origin` -> `https://github.com/google/gemma.cpp.git` (do not push LocalForge)
- `localforge` -> the user's GitHub repository

There is an unrelated user modification in root `CMakeLists.txt` changing
Highway and SentencePiece tags. Preserve it and do not stage, revert, or commit
it unless explicitly requested. Normal commits must name only intended files.

Local commits contain the app history but cannot be pushed directly as the clean
GitHub repository has unrelated ancestry. Established safe publishing flow:

1. Run tests and `git diff --check`.
2. Stage explicit LocalForge files only; never `git add -A`.
3. Commit locally with repo-local author
   `rangzacap4899-maker <rangzacap4899-maker@users.noreply.github.com>`.
4. Clone `https://github.com/rangzacap4899-maker/LocalForge-AI.git` into a fresh
   `mktemp -d` directory.
5. Copy only the changed files into that clean clone, commit, and push `main`.

Latest commits at handoff:

- Local working history: `598350e Fix UI startup glitch by hiding window until ready`
- Clean GitHub history: `6694199 Fix UI startup glitch by hiding window until ready`

The hashes differ because of the separate histories; the file contents should
match. Never force-push or rewrite the user's GitHub history.

HTTPS push uses the `gh` auth token: `gh auth setup-git` was run once and set
`credential.https://github.com.helper !…/gh auth git-credential` (global git
config). There is no SSH key for GitHub, so pushes must go over HTTPS.

## Known cleanup and next checks

- The app must be restarted after source changes; an already open GUI continues
  running old Python code.
- Multi-agent now supports separate model selections for Planner, Coder, and Reviewer. Models are auto-switched in a batch process during agent execution.
- Reviewer and Coder system prompts enforce full complete implementations rather than code stubs (`// ...`), preventing models from skipping work.
- RAG (Retrieval-Augmented Generation) has been integrated in `localforge_algorithms.py` and `_agent_loop` for local intelligent search.
- Streaming UI logic was fixed to properly yield tool loops output without freezing.
- Verify image thumbnails with a newly attached image after restart. Old image
  messages cannot display because their paths were never saved.
- The UI currently mixes some hard-coded Thai status/error strings with the
  translation catalog. Future polishing can move them into i18n.
- Hard-coded Thai strings that remain are intentional: LLM system/agent prompts
  (`SYSTEM_PROMPT`, `CHAT_PROMPT`, Planner/Coder/Reviewer prompts), ToolError
  messages returned to the model, detection regexes, and internal `RuntimeError`
  detail strings. All user-visible UI/status/notification strings now go through
  `self._t(...)`.
- `self._t()` reads a plain `self._lang_code` str attribute (updated only on the
  main thread) rather than a Tk `StringVar`, so it is safe to call from
  `_agent_loop` worker threads.
- Speech output quality depends on installed system voices.
- Continue keeping prompts compact for 4B-class local models.
- `localforge_algorithms.py` dead code was removed: `TaskPlanner`,
  `estimate_complexity`, the unused `threading` import, and the literal `\\n`
  escapes inside the old planner. `VectorDB` gained `counts()`, `clear_rag()`,
  `clear_cache()` for the RAG manager UI.
- The `_open_rag_manager` window was added in `chatbot_app.py`; it is wired to
  the sidebar `rag_manage` button.

## Security, persistence, and packaging hardening (2026-08-07)

- State JSON files and backup manifests now use `atomic_write_text()` with
  `0600` permissions, reducing corruption risk after an interrupted write and
  preventing other local users from reading private state. The application
  state directory is set to `0700`; audit logs are also forced to `0600`.
- Web `fetch_url`/search downloads reject credentials, loopback, private,
  link-local, reserved, multicast, and unspecified IP destinations. Redirects
  are checked with the same policy. This is a DNS preflight guard; it does not
  replace OS/network isolation for hostile DNS rebinding environments.
- RAG re-indexing replaces chunks for the same source instead of accumulating
  duplicates, and an index on `rag_chunks.source` supports source management.
- The tracked desktop entry is now an explicit template (`Exec=localforge-ai`);
  run `./install.sh --skip-deps` to generate the correct absolute-path entry.
  Do not copy `packaging/localforge-ai.desktop` directly.
- Regression coverage now includes private-network URL rejection, atomic file
  mode/write behavior, and duplicate-free RAG re-indexing. Compile, 50 unit
  tests, `git diff --check`, and headless UI boot passed after these changes.
- A live model E2E run was intentionally not started during this handoff. If
  an owned `llama-server` is available, run `xvfb-run -a .venv/bin/python
  python/e2e_smoke.py` after checking port 8080 and the server log.

## Qwen3.5 coding-response compatibility (2026-08-07)

- Qwen3.5 4B on the user's running llama.cpp server reached `n_predict=4096`
  with `reasoning_format=deepseek` and returned no usable `<file>` block. The
  server was healthy; this matches an upstream llama.cpp/Qwen3.5 behavior where
  per-request `enable_thinking=false` may be ignored or reasoning may arrive in
  `reasoning_content`.
- `GemmaClient._read_stream()` now accepts both `delta.content` and
  `delta.reasoning_content`/`delta.reasoning`. If content is empty it extracts
  only the final text after `</think>` and never displays hidden reasoning.
  Non-streaming responses use the same `visible_reasoning_response()` helper.
- Regression coverage includes reasoning-channel extraction. Compile and 51
  unit tests pass after this fix.
- The GUI must be restarted to load this Python change. Do not kill the user's
  currently running llama-server; stop the request normally, close the app,
  then launch it again and retry the coding request.
- `_summarize_context()` now returns a bool: `True` when it completed
  synchronously (local fallback truncation), `False` when an async LLM summary
  is running. `send()` checks the return value and defers itself via
  `_pending_send` (re-calls `self.send(ignore_cache)` after the summary is
  applied on the main thread via `self.after`). The context-inspector button
  ignores the return value.
- `install.sh` sanity check was fixed: the import probe now inserts the
  `python/` directory into `sys.path` (`import customtkinter, localforge_i18n`
  used to fail with a false negative because `localforge_i18n` lives under
  `python/`). `./install.sh --skip-deps` verified on this machine: desktop entry
  written, `Python imports OK`, `Unit tests OK`.
- Installer sanity notes: `install.sh` uses `dnf` on this machine; `apt`/`pacman`
  branches are untested. Desktop entry is regenerated with the real repo path.
  The dnf font package names were corrected after a real `dnf list` check: Fedora
  uses `google-noto-sans-thai-fonts` + `google-noto-sans-cjk-fonts` (the old
  Debian-style `noto-sans-thai-fonts`/`noto-sans-cjk-sc-fonts`/`noto-sans-cjk-jp-fonts`
  do not resolve on Fedora 43). All 15 dnf packages now resolve via `dnf list`.
  The full `sudo dnf install` step still needs an interactive password and has
  not been executed from the agent shell.
- After every material fix, update this file's date, verified test count, recent
  bug notes, and latest local/GitHub commits.

## Complete Modern UX/UI Redesign (2026-08-07)

- Redesigned the complete CustomTkinter desktop interface into a modern, 4-pane
  multi-column IDE layout matching high-end AI code assistant designs.
- Added Far-Left Icon Dock (`dock_frame`) with quick navigation icons for Chat,
  Workspace, RAG Manager, Project Explorer, Settings, and live Theme toggle.
- Upgraded Left Sidebar with search bar, active conversation highlight styling,
  and local privacy badge (`🛡 Local-first & private · ข้อมูลอยู่ในเครื่องทั้งหมด`).
- Built Right Inspector Panel (`inspector`) featuring:
  - Model & Hardware card with model status and live VRAM visual progress bar.
  - Context Usage breakdown card with live token meter progress bar.
  - Project Files tree panel allowing fast workspace file browsing and opening.
- Added Global Bottom Status Bar (`status_bar`) showing Git branch (`🌿 main`),
  workspace folder name, and live hardware summary (CPU, RAM, GPU, VRAM).
- Updated `localforge_i18n.py` with matching keys across all 4 language catalogs (`th`, `en`, `zh`, `ja`).
- Verified: `python3 -m py_compile python/*.py`, 51 passing unit tests, clean
  `git diff --check`, and successful Xvfb headless boot check.
- Enhanced UX/UI to match reference IDE mockup pixel-for-pixel:
  - Added `ContextDonutChart` canvas widget for circular ring context meter with legend breakdown (Code/Files/History).
  - Enhanced Diff Approval dialog & cards (`Plan: X files to change ∨`, `👁 Review`, `✓ Apply`, `+XX` green line counters, code box line numbers).
  - Added `Files` | `Symbols` tab header to Project Files panel.
  - Added bottom status bar action buttons (`🌿 main ∨`, `✓ No changes`, `▷ Run ∨`, `👁 Review`, `↶ Undo`).
- De-duplicated & Streamlined UI controls:
  - Removed duplicate theme toggle and context pill from Main Header (kept in Dock and Right Inspector Panel).
  - Removed duplicate hardware monitor grid from Left Sidebar (kept in Right Inspector Panel and Bottom Status Bar).
  - Streamlined Header bar to contain Session Title, Model Selector Pill (`Qwen3.5 4B ∨`), and Status Badge (`● Local GPU`).

## Advanced Feature Suite (2026-08-07)

- **Command Palette (`Ctrl+K` / `Ctrl+P`)**: Popup modal dialog with real-time search across commands, workspace files, installed models, and conversations.
- **Global Hotkeys**: `Ctrl+N` (New Chat), `Ctrl+B` (Toggle Sidebar), `Ctrl+Shift+F` (Workspace File Search).
- **Side-by-Side Diff Viewer**: Dual-pane `Original` vs `Modified` preview mode in the file approval dialog.
- **Auto Background RAG Indexing**: `_auto_index_workspace()` runs on background thread upon workspace selection.
- **Code Execution Sandbox (`▷ Run Code`)**: Direct execution of Python/Shell code blocks in workspace with timeout and output viewer window.
- **Git Status Badges**: `_refresh_project_file_list()` queries `git status --porcelain` and displays `M` (amber), `A` (green), `D` (red) badges on project file tree.
- **Embedded IDE Subsystem (`CodeEditorPanel`)**:
  - Full-featured code editor panel built into app with multi-tab file management, line numbers gutter (`1, 2, 3...`), `Ctrl+S` atomic save with backups, and `✨ Ask AI about this file`.
  - 3-Way View Modes (`_set_view_mode`): `💬 Chat View`, `💻 IDE View`, `◫ Split View` (IDE Code Editor + AI Assistant side-by-side).
  - Project Files tree integration: Clicking any project file opens it directly in the Embedded IDE tabs.
- **100% Guaranteed Icon Rendering Solution (BMP Codepoint Migration)**:
  - Identified root cause of invisible icons on Bazzite Linux: Tkinter X11 font engine does not support SMP (> U+10000) 4-byte Color Emojis.
  - Replaced all SMP emoji codepoints with high-precision BMP (U+0000..U+FFFF) symbols: `✉` (Chat), `⌨` (IDE), `◫` (Split), `❏` (Workspace), `▧` (RAG), `⚙` (Settings), `◐` (Theme), `♟` (User Avatar), `⎇` (Git Branch).
  - Ensured 100% visible, crisp icon rendering across all Linux, Fedora, Bazzite, Ubuntu, and Windows systems.
- Verified: `python3 -m py_compile python/*.py`, 51 passing unit tests, clean `git diff --check`, and Xvfb headless boot check.

## Full UX/UI Overhaul (2026-08-07)

- **Active Dock State**: `_dock_buttons: dict[str, CTkButton]` tracks Chat/IDE/Split buttons; `_set_view_mode()` highlights the active button with `ACCENT` background + white text and dims all others to transparent.
- **Premium Chat Bubbles**: Each message row uses a 3-column grid (avatar | bubble | avatar). User bubbles show `♟` avatar (BMP) + soft indigo `USER_BUBBLE` bg + ACCENT border. AI bubbles show `✦` avatar + `BOT_BUBBLE` bg. Error/system bubbles use colour-coded backgrounds.
- **BMP Icon Fixes Throughout**:
  - `_refresh_project_file_list`: dir=`❏`, python=`▷`, file=`▤`
  - `CodeEditorPanel._render_tabs`: file tab icon `▤` (was SMP `📄`)
  - Inspector model card header: `⬡` (BMP, accent-colored, was SMP `🧊`)
  - Sidebar workspace button: `❏` (was SMP `📁`)
  - Status bar Git branch: `⎇` (already BMP, confirmed)
- **Composer Redesign**: Input box height 90px (was 75px), taller Send/Stop buttons 86px (was 44px), spanning all 3 rows, cleaner separator line, attach/voice buttons use `MUTED` text color + `+`/`⏺` prefixes.
- **New Chat Button**: `"+ " + new_chat` label with subtle `ACCENT_SUBTLE` fill and ACCENT border for standout appearance.
- **Privacy Badge**: Sidebar privacy card border uses `(light=#94A3B8, dark=#10B981)` — green in dark mode for premium look.
- **Donut Chart Enlarged**: Canvas 106x106 (was 90x90), arc coords 8,8,98,98, ring width 10, text at center 53,45/53,61.
- **Premium Empty State UI**: When no messages exist, chat area shows a centered `72px ✦ logo + title + tagline + shortcut tips card` instead of a plain system message. Rebuilt on `_clear()` / conversation switch via `_render_messages()`.
- Last updated: 2026-08-08 (Asia/Bangkok) — seventh handoff
- Verified: `python3 -m py_compile python/*.py`, 51 passing unit tests, clean `git diff --check`, Xvfb headless boot with view mode switching passed.

## Compact Dock, IDE Smart Assistants & Validation Fixes (2026-08-08)

- **Dock & Composer Layout Refinement**:
  - Fixed Dock width locking via `dock.pack_propagate(False)` set to 44px with compact 34x34px icons.
  - Streamlined Composer height and Send/Stop buttons for a tighter, compact feel.
- **IDE Subsystem Overhaul (`CodeEditorPanel`)**:
  - Added tab close button (`×`) with `close_file()` handling for multi-tab management.
  - Redesigned action buttons (`💾 Save` & `✦ Ask AI`).
  - Made line numbers gutter background theme-aware (`SIDEBAR` / `MUTED`).
  - Added Smart Auto-Indentation on `<Return>` (copies leading indent + adds 4 spaces after `:`).
  - Added Auto-Closing Pairs for `()`, `[]`, `{}`, `""`, `''` with step-over capability.
  - Added debounced regex-based Syntax Highlighting for Python keywords, strings, and comments.
- **Lenient File Validation (`localforge_core.py`)**:
  - Relaxed strict `</html>` and JS/CSS bracket validation in `FileTransaction.validate()`, preventing popup error blocks when local models output incomplete code.
- **Inspector & Model Sync**:
  - Bound `right_model_name_label` to `selected_model_var` via `textvariable` so right inspector always stays synced with top dropdown selection.
  - Removed redundant "Project Files" card from right inspector panel.
- Verified: `python3 -m py_compile python/*.py`, 51 passing unit tests, clean `git diff --check`, Xvfb headless boot test passed.

## Defect-hunting fix pass (2026-08-08)

Full read-through of `chatbot_app.py`/`localforge_core.py` plus pyflakes and the
51-test suite. All fixes below are verified (compile + 51 tests + `pyflakes` for
the app/core modules).

- **Run Code button fixed**: `_run_code_snippet` used `sys.executable` but `sys`
  was never imported — every "▷ Run Code" raised `NameError`. Added `import sys`.
- **Auto RAG indexing now actually works**: `_auto_index_workspace` called a
  non-existent `vector_db.add_rag(...)` with a wrong `get_embedding(block)` call
  (missing `api_url`); both errors were silently swallowed by `except: pass`.
  It now passes `api_url` and uses the existing `replace_rag_source(rel, chunks)`
  for duplicate-free per-document re-indexing.
- **Duplicate `_open_path` removed**: the method was defined twice in `ChatApp`;
  the later definition (external `xdg-open`) shadowed the earlier IDE one, so
  sidebar/project-explorer file clicks never opened the Embedded IDE. There is
  now a single merged method: directory → project explorer, `.html`/`.htm` →
  browser (keeps "Open Index" preview working), everything else → `code_editor`
  + split view.
- **Busy guards added** to `_clear`, `_delete_message`, `_edit_message`,
  `_regenerate_message` so message list mutations can no longer race with the
  `_agent_loop` worker thread during generation.
- **RAG no longer pollutes saved history**: the augmented prompt was written
  back into `self.messages[i]["content"]` and persisted by `_save_history`.
  It now builds a working copy (`rag_messages`) used only for the model context
  (first tool round and the file-generation request path).
- **View-mode grid overlap fixed**: `editor`/`split` modes now `grid_remove()`
  the sidebar and inspector instead of letting the editor frame overlap them;
  `_responsive_layout` respects `_current_view_mode` before restoring the
  inspector.
- **Model info now resolves nested paths**: `_model_selected` built
  `root / basename` for models living in subdirectories, so the inspector always
  said "not found". It now uses `_resolve_model_path()` like `_load_selected_model`.
- **`FileTransaction.apply` is atomic**: validates the whole batch before writing
  any file, so a late validation failure can no longer leave partial writes
  without a manifest to undo.
- **Diff review shows real content**: the side-by-side window showed the same
  unified diff in both panes; it now shows actual per-file original vs modified.
- **Command palette fixes**: rows only bound the frame (clicking the text did
  nothing) — labels are now click-bound too; the workspace `rglob("*")[:30]`
  full-tree walk was replaced with a bounded `os.walk` (same for the Project
  Explorer, capped at 1200 entries) to stop UI freezes on large `$HOME` trees.
- **Sidebar git badges are directory-aware**: top-level dir entries now show
  M/A/D when a nested file changed instead of never matching.
- **SMP emoji cleanup completed**: the BMP migration had left stragglers
  (`💾 📝 🎯 📁 💬 📚 📄 🧊` in the app, `🎙 🔊 📎 👁 💻 🗂 📚 📁` in the
  i18n catalogs). All replaced with BMP symbols (e.g. `▣ ✎ ◎ ❏ ✉ ▧ ▤ ◈ ◉ ♪ ＋
  ▦`). Verified zero non-BMP characters remain in `chatbot_app.py` and
  `localforge_i18n.py`.
- **Minor cleanups**: unused `temperature` var in `_update_system_monitor`
  removed; dead `if False else` placeholder removed; `ContextDonutChart` canvas
  bg now follows the parent card theme instead of hardcoded dark; recording stop
  resets the media status label; unused imports/vars in `localforge_core.py`
  removed; unused `answer` var in benchmark removed.
- Verified: `py_compile` OK, 51 unit tests OK, `pyflakes` clean for
  `chatbot_app.py` and `localforge_core.py` (only benign unused imports remain in
  the test files), no non-BMP chars. Not yet committed; do not stage
  `CMakeLists.txt`.
